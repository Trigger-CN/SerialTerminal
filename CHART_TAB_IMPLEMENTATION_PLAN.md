# 图表页实现方案

## 1. 背景与目标

SerialTerminal 当前支持主串口终端、过滤页和 Shell 页。计划新增第三种可创建的工作区标签页：图表页。

图表页持续消费串口接收数据，通过自动键值、格式模板或高级正则三种方式解析文本日志行，将用户勾选的一个或多个数值字段转换成数据序列，并以不同颜色的折线实时展示趋势。

目标日志示例：

```text
a: 50, b: 298, c: 2283
a: 53, b: 301, c: 2260
a: 48, b: 295, c: 2301
```

默认使用自动键值模式，无需填写表达式。工具解析第一条样例后列出 `a`、`b`、`c`，由用户勾选需要绘制的字段。

需要严格匹配时可以使用格式模板：

```text
a: {a}, b: {b}, c: {c}
```

高级用户仍可使用正则：

```regex
a:\s*(?<a>[-+]?\d+(?:\.\d+)?),\s*b:\s*(?<b>[-+]?\d+(?:\.\d+)?),\s*c:\s*(?<c>[-+]?\d+(?:\.\d+)?)
```

正则模式下，命名捕获组作为候选字段来源，而不是跳过用户确认直接创建折线。

图表页底部必须提供完整时间轴导航器。时间轴显示当前图表会话的完整数据范围，并包含一个可拖动、可缩放的滑动窗口。上方主折线图只显示滑动窗口覆盖的时间范围。

## 2. 第一版范围

第一版包含：

- 新建、关闭、重命名、拖动、分屏和恢复图表页。
- 默认使用自动键值方式解析常见 `key=value` 和 `key:value` 日志。
- 支持类似 `scanf` 的易读格式模板，例如 `a: {a}, b: {b}`。
- 保留正则表达式作为复杂日志的高级解析方式。
- 解析第一条样例后自动发现字段、样例值、类型和单位。
- 用户勾选候选字段后生成数据系列。
- 每个系列独立设置名称、颜色、单位、精度和可见性。
- 主折线图实时显示滑动窗口对应的数据范围。
- 底部完整时间轴显示当前会话的全局趋势。
- 时间轴窗口支持整体拖动、左右边缘缩放和点击定位。
- 实时跟随、暂停、继续和清空。
- 自动或固定 Y 轴范围。
- 显示当前值、最小值、最大值和平均值。
- 导出当前窗口或全部原始保留数据为 CSV。
- 图表配置持久化，应用重启后恢复。
- 样例日志实时解析、字段发现、格式验证和预览。

第一版不包含：

- 从终端滚动缓冲区重新解析历史数据。
- 从文本日志文件离线导入。
- 一个图表页同时运行多个解析规则。
- 派生字段和任意数学表达式。
- 多 Y 轴。
- 报警规则和自动触发操作。
- 二进制协议字段解析。
- 应用重启后恢复上次数据点。

## 3. 日志格式契约

### 3.1 推荐格式与解析模式

设备应输出稳定的一行一条样本的键值日志：

```text
TEMP: 25.6, HUM: 48.2, PRESSURE: 101.32
```

自动键值模式可直接识别这些字段。需要限定固定格式时，推荐模板：

```text
TEMP: {temperature}, HUM: {humidity}, PRESSURE: {pressure}
```

日志约束：

- 每个样本占一行。
- 字段名称应保持稳定；自动键值模式允许顺序变化。
- 数值使用半角数字和小数点。
- 数值可紧跟单位，但不应加入千位分隔符。
- 每行使用 `CRLF`、`LF` 或 `CR` 结束。
- 行首和行尾允许存在其他文本。
- 单位可以自动推断，并在字段配置中确认或修改。

### 3.2 支持的数值

支持整数、正负数、小数和科学计数法：

```text
50
-12
+3.14
.25
1.2e3
-4.5E-2
```

解析后的数值必须是有限值。`NaN`、`Infinity` 和 `1,024` 默认视为非法值。自动键值和模板模式支持将 `93600us` 拆分为数值 `93600` 与单位 `us`；十六进制仅在显式 `{value:hex}` 模板类型下支持。

### 3.3 字段定义

自动键值模式从样例中的键名生成候选字段；格式模板从命名占位符生成候选字段；正则模式要求至少包含一个命名捕获组：

```regex
(?<temperature>...)
```

字段名称作为稳定的系列键，建议限定为：

```text
[A-Za-z_][A-Za-z0-9_]*
```

正则模式不使用普通位置捕获组生成系列，因为规则修改后位置容易错位，也无法稳定保存系列名称、颜色和单位。最终只有用户勾选并保存的字段才成为活动系列。

### 3.4 缺失值与非法值

已配置字段没有值时，该系列在当前时间点记录 `null`，折线在此处断开，不以 `0` 代替。

以下情况拒绝整行：

- 行标记或解析规则不匹配。
- 所有活动字段均为空。
- 任一非空活动字段不能转换为有限数值。
- 解析执行失败，或正则模式的 Worker 超时。

### 3.5 示例格式

如果日志为：

```text
a: 50吗, b: 298, c: 2283
```

且“吗”固定存在，可以使用格式模板：

```text
a: {a}吗, b: {b}, c: {c}
```

更推荐设备输出无额外文本的稳定格式：

```text
a: 50, b: 298, c: 2283
```

## 4. 页面布局

图表页采用紧凑的工具型布局：

```text
┌─────────────────────────────────────────────────────────────┐
│ 解析规则摘要          [设置] [暂停] [清空] [导出] [回到实时] │
├─────────────────────────────────────────────────────────────┤
│ ● A 50.0   ● B 298.0   ● C 2283.0   匹配 1320  丢弃 0      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                     主折线图                                │
│          仅显示底部滑动窗口覆盖的时间范围                   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ 完整时间轴概览                                               │
│ 10:00  ┃━━━━━━[══════════ 可视窗口 ══════════]━━━━┃ 10:30  │
│              ◀ 左缩放柄                      右缩放柄 ▶      │
└─────────────────────────────────────────────────────────────┘
```

顶部配置区默认折叠，避免长期占用绘图空间。设置面板用于配置解析方式、样例日志、字段选择、编码、Y 轴、数据保留和时间轴行为。

### 4.1 样例日志与字段发现

新建图表页的默认流程：

```text
选择解析方式 -> 粘贴一条样例 -> 自动发现字段 -> 勾选系列 -> 确认单位/颜色 -> 保存
```

用户也可以从主终端选择一行，通过右键菜单“从该行创建图表”自动填入样例。解析方式提供三级选择：

- 自动键值：默认方式，识别 `key=value`、`key:value` 和数值后的单位。
- 格式模板：推荐的精确模式，使用 `{field}` 或 `{field:type}` 占位符。
- 正则表达式：仅供复杂日志使用的高级模式。

### 4.2 `[FRAME_PERF]` 样例

样例日志：

```text
frame_perf_disp_event_cb: [FRAME_PERF] count=321 frame_total=93600us layout_prep=5300us render_envelope=39300us flush=28300us draw=11000us post_render=49000us mo_dual_disp.c:187
```

自动键值配置：

```text
行标记：[FRAME_PERF]
键值分隔符：=
字段分隔方式：空白
单位识别：自动
```

工具解析样例后显示候选字段表：

| 绘图 | 字段 | 样例值 | 推断类型 | 原始单位 | 显示单位 |
|---|---|---:|---|---|---|
|  | `count` | 321 | 整数 | 无 | 无 |
| ✓ | `frame_total` | 93600 | 数值 | `us` | `ms` |
| ✓ | `layout_prep` | 5300 | 数值 | `us` | `ms` |
| ✓ | `render_envelope` | 39300 | 数值 | `us` | `ms` |
| ✓ | `flush` | 28300 | 数值 | `us` | `ms` |
| ✓ | `draw` | 11000 | 数值 | `us` | `ms` |
| ✓ | `post_render` | 49000 | 数值 | `us` | `ms` |

函数名前缀和末尾源码位置不符合 `key=value`，不会被误判为字段。`[FRAME_PERF]` 作为行标记，避免其他日志中的键值污染该图表。

### 4.3 勾选与推断规则

- 带相同时间单位的数值字段默认勾选。
- `count`、`index`、`seq`、`id` 等计数或标识字段默认不勾选，但允许手动选择。
- 文本字段不能直接生成折线。
- `count` 可指定为样本序号，用于发现跳号，但不与耗时共用 Y 轴。
- 用户可修改显示名称、颜色、原始单位、显示单位和精度。
- 超过 16 个候选数值字段时不自动全选，并提示系列数量上限。
- 字段发现只生成候选配置；后续日志出现新字段时不能静默新增折线。

### 4.4 格式模板

该样例对应的严格模板为：

```text
[FRAME_PERF] count={count:int} frame_total={frame_total:us} layout_prep={layout_prep:us} render_envelope={render_envelope:us} flush={flush:us} draw={draw:us} post_render={post_render:us}
```

支持 `{value}`、`{value:number}`、`{value:int}`、`{value:hex}`、`{state:text}`、`{timestamp:time}` 及 `{value:us}`、`{value:ms}` 等单位类型。模板默认行内匹配，普通空白宽松匹配。实现使用固定文本和字段 token 顺序扫描，不依赖复杂回溯正则。

### 4.5 保存校验

保存前必须满足：样例解析成功、至少发现一个字段、至少勾选一个数值字段、字段名称唯一、单位换算有效且活动系列不超过 16 个。面板实时显示匹配结果和转换后的首个样本；保存后开始新的图表数据会话。

## 5. 完整时间轴与滑动窗口

### 5.1 核心模型

图表同时维护两个时间范围：

```text
fullRange = [sessionStart, latestTimestamp]
viewRange = [viewStart, viewEnd]
```

`fullRange` 表示当前图表数据会话的完整时间范围。底部时间轴始终映射这个范围。

`viewRange` 表示滑动窗口覆盖的时间范围。上方主图的 X 轴必须与 `viewRange` 完全一致。

窗口宽度与主图显示时长成正比：

```text
windowPixelWidth / timelinePixelWidth
    = (viewEnd - viewStart) / (fullEnd - fullStart)
```

当窗口长度缩短时，主图放大到更短的时间范围；窗口长度增加时，主图缩小并显示更长的时间范围。

### 5.2 时间轴内容

底部时间轴不是空白滚动条，而是完整数据概览图：

- 横向覆盖当前会话完整时间范围。
- 使用所有可见系列的低成本概览折线或包络线。
- 对长时间数据使用分层降采样摘要。
- 当前滑动窗口内区域保持正常亮度。
- 窗口外区域使用半透明遮罩。
- 左右边缘显示清晰的缩放手柄。
- 标注完整范围起止时间和窗口当前时长。

时间轴高度建议为 `72px` 至 `96px`，其中包括概览图、窗口和时间刻度。

### 5.3 窗口交互

支持以下交互：

- 拖动窗口主体：保持窗口时长不变，整体移动 `viewRange`。
- 拖动左手柄：修改 `viewStart`，保持 `viewEnd` 不变。
- 拖动右手柄：修改 `viewEnd`，保持 `viewStart` 不变。
- 点击窗口外区域：以点击时间为中心移动窗口。
- Shift + 滚轮：以鼠标位置为中心缩放窗口长度。
- 主图滚轮：同步缩放底部窗口。
- 主图拖动：同步平移底部窗口。
- 双击时间轴或点击“回到实时”：恢复默认实时窗口。

拖动过程使用 `requestAnimationFrame` 合并更新，避免每个 `pointermove` 都触发完整绘图。

### 5.4 缩放边界

建议默认值：

```text
默认窗口：最近 60 秒
最小窗口：1 秒
最大窗口：完整会话范围
最小窗口像素宽度：24px
```

边界规则：

- `viewStart` 不得早于 `fullStart`。
- `viewEnd` 不得晚于 `fullEnd`，实时跟随的短暂前置留白除外。
- `viewEnd - viewStart` 不得小于最小窗口时长。
- 会话总时长小于默认窗口时，窗口覆盖全部数据。
- 缩放以当前操作手柄或鼠标指向时间为锚点。

### 5.5 实时跟随

新增数据到达时分为两种状态。

实时跟随开启：

```text
viewEnd = latestTimestamp
viewStart = viewEnd - currentWindowDuration
```

窗口在底部时间轴中保持贴近右侧，窗口长度不变。

用户手动向历史区域拖动窗口后：

- 自动关闭实时跟随。
- 保持当前 `viewRange`，新数据只扩展 `fullRange`。
- 底部窗口会逐渐远离时间轴右侧。
- 显示“回到实时”按钮和未查看的新样本数量。

点击“回到实时”后：

- 保留当前窗口时长。
- 将窗口移动到最新数据。
- 恢复实时跟随。

### 5.6 暂停与时间轴

暂停图表时：

- 不再向图表数据模型追加新样本。
- 主串口接收、主终端、过滤页和自动日志不受影响。
- `fullRange` 和 `viewRange` 保持不变。
- 时间轴显示暂停状态。

第一版暂停期间的数据不补录。继续后从新的串口记录开始追加，并在时间轴上保留真实时间间隔，因此折线会出现时间空档。

### 5.7 时间精度与相同时间戳

时间戳应尽量在主进程 `port.on('data')` 时生成，并随 IPC 数据传递。每条解析记录还应带单调递增 `sequence`。

如果同一串口批次中的多条日志共享时间戳，使用 `sequence` 保证顺序。绘图库数据的 X 值必须严格递增时，可在显示层增加极小偏移，但 CSV 仍导出原始时间戳和序号。

## 6. 数据保留模型

底部时间轴要求展示完整会话趋势，但不能因此无限保留所有原始样本。采用双层数据保留模型。

### 6.1 原始数据层

原始数据用于：

- 上方主图的精确显示。
- Tooltip 和统计。
- CSV 导出。

建议默认限制：

```text
最大原始点数：200,000
最大原始保留时长：30 分钟
```

两项限制同时生效，达到任一限制后淘汰最旧原始点。

### 6.2 完整时间轴摘要层

摘要层覆盖当前会话从开始到最新数据的完整范围，即使早期原始点已经淘汰，也保留其趋势摘要。

摘要采用分层时间桶：

```text
Level 0: 100ms 桶
Level 1: 1s 桶
Level 2: 10s 桶
Level 3: 1min 桶
```

每个系列、每个桶保存：

```js
{
  startTime,
  endTime,
  first,
  last,
  min,
  max,
  sum,
  count
}
```

时间轴按当前画布宽度选择合适层级，每个水平像素最多绘制少量摘要点。使用 `min/max` 包络保留短时尖峰，不能只保存平均值。

### 6.3 原始数据已淘汰的窗口

用户可以把时间轴窗口移动到仅有摘要、没有原始点的历史区域。

此时：

- 上方主图使用摘要数据绘制趋势。
- 页面显示“该范围为降采样历史数据”。
- Tooltip 显示桶的最小值、最大值和平均值，而不是伪装成精确采样值。
- CSV 的“当前窗口”导出仅包含仍存在的原始点。
- 若窗口完全没有原始点，禁用精确 CSV 导出并说明原因。

这种语义比限制时间轴只能显示原始保留范围更符合“完整时间轴”的产品要求，也避免内存无限增长。

### 6.4 会话边界

图表数据会话从以下任一事件开始：

- 创建图表页并开始接收数据。
- 用户点击清空。
- 用户修改正则、编码或字段定义并保存。
- 串口重新连接且配置选择“重连时新建图表会话”。

第一版建议默认在串口重连时开始新会话并清空数据，避免不同设备连接或设备重启前后的时间轴被错误连成一个连续趋势。

## 7. 主图与时间轴同步

上方主图和底部时间轴共享同一个 `ChartViewportController`：

```js
{
  fullStart,
  fullEnd,
  viewStart,
  viewEnd,
  followLatest,
  minWindowDuration,
  defaultWindowDuration
}
```

所有修改必须通过控制器方法完成：

```js
setFullRange(start, end)
setViewRange(start, end, source)
panView(deltaMs, source)
zoomView(anchorTime, scale, source)
resizeLeft(time)
resizeRight(time)
followLatest()
```

`source` 用来防止主图和时间轴互相递归触发更新，例如 `main-chart`、`navigator`、`live-data`。

主图数据选择流程：

1. 根据 `viewRange` 二分查找原始数据索引。
2. 如果范围内原始点存在，提取原始数据。
3. 根据主图像素宽度执行 min/max 桶降采样。
4. 如果范围早于原始数据起点，从摘要层获取对应历史。
5. 合并摘要和原始区间时明确标记数据精度边界。
6. 调用 uPlot `setData()`，再同步 X 轴范围。

时间轴数据选择流程：

1. 使用完整 `fullRange`。
2. 根据时间轴画布宽度选择摘要层级。
3. 绘制全部会话概览。
4. 根据 `viewRange` 计算窗口像素位置和宽度。
5. 更新遮罩、手柄和时间标签。

## 8. 图表库选择

推荐使用 uPlot：

- 适合高性能时序折线图。
- 大量数据点下开销较低。
- 支持多系列、游标、缩放和动态更新。
- Canvas 渲染适合持续追加数据。
- 可以通过插件实现主图和时间轴导航器同步。

建议使用两个 uPlot 实例：

- 主图实例：显示 `viewRange`，支持 Tooltip、游标和缩放。
- 时间轴实例：显示 `fullRange`，禁用普通游标交互，由自定义窗口覆盖层处理拖动和缩放。

不建议直接依赖 uPlot 内部未公开状态实现窗口，滑动窗口应使用独立 DOM 覆盖层和统一控制器，便于触摸、键盘和可访问性支持。

## 9. 串口数据接入

图表不能从 xterm 缓冲区、过滤页输出、Hex 展示文本或自动日志文件中读取数据。

推荐链路：

```text
serialport Buffer
  -> main.js IPC batch
  -> renderer receive pipeline
  -> 流式解码
  -> 按行分帧
  -> TextRecord fan-out
       -> 主终端
       -> 过滤页
       -> 图表解析 Worker
       -> 自动触发
```

统一文本记录结构：

```js
{
  sessionId: 12,
  sequence: 4831,
  receivedAt: 1786592011125.42,
  text: 'a: 50, b: 298, c: 2283',
  delimiter: '\r\n',
  partial: false
}
```

图表数据源应独立于主终端的 Text/Hex 显示模式。主终端切换到 Hex 时，图表仍按照图表页保存的文本编码继续解析。

## 10. 流式解码与分帧

实施图表功能前，应将文本解码和行分帧提取为独立模块 `serial-text-stream.js`。

接口建议：

```js
class SerialTextStream {
  constructor({ encoding, maxLineLength })
  write(bytes, receivedAt)
  flush()
  reset()
  onRecord(callback)
}
```

要求：

- 正确处理跨 chunk 的 UTF-8 和 GBK 多字节字符。
- 正确处理跨 chunk 的 `CRLF`。
- 支持 `CRLF`、`LF` 和独立 `CR`。
- 限制单行最大长度，例如 64 KiB。
- 超长行标记为 `partial`，默认不送入图表解析器。
- 断开连接时处理剩余文本。
- 重连时清除旧 decoder 和未完成行。
- 每条记录携带时间、会话 ID 和序号。

## 11. 日志解析 Worker

自动键值、格式模板和正则执行统一放入 `chart-parser-worker.js`，共享批处理、字段转换、单位换算和过载保护，并避免高级正则或高频日志阻塞 Renderer。

主线程发送：

```js
{
  type: 'records',
  tabId,
  records: [
    { sequence, receivedAt, text }
  ]
}
```

Worker 返回：

```js
{
  type: 'samples',
  tabId,
  samples: [
    {
      sequence: 4831,
      timestamp: 1786592011125.42,
      values: { a: 50, b: 298, c: 2283 }
    }
  ],
  stats: {
    received: 100,
    matched: 95,
    rejected: 5
  }
}
```

解析保护：

- 正则长度最多 2,000 字符。
- 单行最多 64 KiB。
- 单批最多 500 行或 256 KiB。
- Worker 看门狗建议 500 ms。
- 超时后终止并重建 Worker。
- 当前图表页进入错误暂停状态。
- 输入队列必须有界，过载时丢弃最旧未解析记录并计数。

正则模式第一版只开放忽略大小写，内部固定启用 Unicode。每行只产生一个样本，不开放全局多匹配模式。自动键值和格式模板使用顺序扫描，不生成容易灾难性回溯的复杂正则。

## 12. 图表运行时数据模型

```js
{
  id: 'tab-chart-1',
  paneId: 'pane-1',
  title: 'Chart 1',
  parserMode: 'key-value',
  sampleLine: '...',
  lineMarker: '[FRAME_PERF]',
  keyValueSeparator: '=',
  pattern: '...',
  caseInsensitive: false,
  encoding: 'utf8',
  xAxisMode: 'time',
  series: [
    {
      key: 'a',
      label: 'A',
      color: '#4FC3F7',
      unit: '',
      visible: true,
      precision: 2
    }
  ],
  viewport: {
    fullStart: null,
    fullEnd: null,
    viewStart: null,
    viewEnd: null,
    followLatest: true,
    defaultWindowDuration: 60000,
    minWindowDuration: 1000
  },
  retention: {
    rawMaxPoints: 200000,
    rawDurationMs: 1800000
  },
  paused: false,
  rawTimestamps: [],
  rawValuesBySeries: new Map(),
  summaryLevels: [],
  stats: {
    receivedLines: 0,
    matchedLines: 0,
    rejectedLines: 0,
    droppedSamples: 0
  },
  mainChart: null,
  timelineChart: null,
  resizeObserver: null
}
```

## 13. 持久化配置

只持久化配置，不持久化历史数据、缩放位置和暂停状态：

```json
{
  "id": "tab-chart-1",
  "paneId": "pane-1",
  "title": "传感器趋势",
  "parserMode": "key-value",
  "sampleLine": "[FRAME_PERF] a=50us",
  "lineMarker": "[FRAME_PERF]",
  "keyValueSeparator": "=",
  "caseInsensitive": false,
  "encoding": "utf8",
  "viewport": {
    "defaultWindowDuration": 60000,
    "minWindowDuration": 1000
  },
  "retention": {
    "rawMaxPoints": 200000,
    "rawDurationMs": 1800000
  },
  "series": [
    {
      "key": "a",
      "label": "A",
      "color": "#4FC3F7",
      "unit": "",
      "visible": true,
      "precision": 2
    }
  ]
}
```

配置 schema 版本需要递增。主进程负责校验解析模式、样例长度、模板或正则长度、字段定义、编码枚举、系列数量、颜色格式、单位换算、窗口时长、原始点上限和 Pane ID。非正则模式不保存无意义的 `pattern`。

## 14. 多系列管理

解析样例时生成候选字段，用户确认并勾选后创建系列。修改解析配置后重新运行字段发现：

候选字段从 `a,b,c` 变为 `a,c,d` 时：

- 保留 `a`、`c` 的颜色、名称和单位。
- 删除活动系列 `b`。
- 为 `d` 分配新颜色。
- 保存后开始新图表会话并清空旧数据，避免不同字段定义混合。

建议调色板：

```text
#4FC3F7
#66BB6A
#FFCA28
#EF5350
#AB47BC
#26A69A
#FFA726
#EC407A
```

第一版最多允许 16 个系列。

## 15. Y 轴与统计

第一版提供：

- 自动范围。
- 固定最小值和最大值。
- 自动范围包含零点开关。
- 上下边距比例。

主图统计默认基于当前 `viewRange` 内的原始数据：

- 当前值。
- 最小值。
- 最大值。
- 平均值。

窗口包含摘要历史时，统计区域必须标记为估算值。量纲差异过大的字段建议放到不同图表页，多 Y 轴留到后续版本。

## 16. CSV 导出

提供两个导出范围：

- 导出当前滑动窗口。
- 导出全部原始保留数据。

格式：

```csv
timestamp,sequence,a,b,c
2026-08-13T10:24:31.125Z,1,50,298,2283
2026-08-13T10:24:31.159Z,2,53,301,2260
```

规则：

- 使用 UTF-8 with BOM。
- 时间使用 ISO 8601。
- 缺失值输出为空。
- 导出原始数据，不导出绘图降采样数据。
- 已淘汰、只存在摘要的数据不能作为精确 CSV 导出。
- 导出当前窗口时明确提示窗口中是否包含不可导出的摘要历史。

## 17. 工作区集成

图表页作为完整第三类标签接入现有工作区。

新增运行时状态：

```js
let chartTabs = [];
let chartTabCounter = 1;
```

新增核心函数：

```js
createChartTab()
closeChartTab()
persistChartTabs()
getChartTabState()
clearChartTab()
resizeChartTab()
exportChartTab()
```

必须接入以下生命周期：

- 新建和配置恢复。
- 标签激活和 resize。
- 拖动排序和跨 Pane 移动。
- 分屏和折叠分屏。
- 重命名和关闭。
- 清空和导出。
- 右键菜单。
- 侧边栏和输入栏尺寸变化。

主进程 Context Menu 类型识别必须显式增加 `chart`，不能让未知类型回退为主终端。

图表右键菜单：

```text
暂停/继续
回到实时
清空数据
导出当前窗口 CSV
导出全部原始数据 CSV
图表设置
移动到另一分屏
关闭图表页
```

## 18. 模块拆分

建议新增：

```text
serial-text-stream.js
chart-regex.js
chart-parser-worker.js
chart-data-model.js
chart-viewport-controller.js
chart-timeline-summary.js
chart-export.js
```

职责：

- `serial-text-stream.js`：流式解码、分帧和时间传播。
- `chart-parser-config.js`：三级解析模式配置、样例字段发现、模板编译和正则验证。
- `chart-parser-worker.js`：自动键值、模板或正则的批量执行、单位换算和数值转换。
- `chart-data-model.js`：原始数据追加、裁剪、统计和查询。
- `chart-viewport-controller.js`：完整范围、滑动窗口、平移、缩放和实时跟随。
- `chart-timeline-summary.js`：分层摘要桶和完整时间轴数据选择。
- `chart-export.js`：CSV 生成和转义。

## 19. 可访问性与键盘操作

时间轴滑动窗口需要可通过键盘操作：

- `Left/Right`：按当前窗口时长的 10% 平移。
- `Shift + Left/Right`：按 50% 平移。
- `+/-`：以窗口中心缩放。
- `Home`：移到会话开始。
- `End`：回到实时。

窗口元素使用 `role="slider"`，并提供当前开始时间、结束时间和窗口时长的 ARIA 描述。左右缩放手柄应具有独立可聚焦控制或等价的键盘操作方式。

## 20. 错误与空状态

图表页必须显示明确状态：

- 尚未配置解析规则。
- 尚未提供样例日志。
- 样例日志没有发现键值字段。
- 没有勾选任何绘图字段。
- 正则语法错误。
- 没有命名捕获组。
- 正则解析超时。
- 串口未连接。
- 已连接但尚无匹配行。
- 图表已暂停。
- 输入过载并丢弃记录。
- 编码无法正确解码。
- 系列数量超过限制。
- 当前窗口只包含降采样摘要。

不能用无说明的空白图表表达错误。

## 21. 测试方案

### 21.1 解析与字段发现测试

- `[FRAME_PERF]` 性能日志自动发现七个键值字段。
- 函数名前缀和源码位置不会被误判为字段。
- `93600us` 正确拆分为数值 `93600` 和单位 `us`。
- `us` 到 `ms` 的显示换算正确。
- 计数和 ID 类字段默认不勾选。
- 后续日志出现新字段时不会静默新增系列。
- 用户字段勾选、名称、颜色和单位配置可以持久化。
- 格式模板固定文本、宽松空白和命名占位符解析正确。
- 单字段和多字段。
- 整数、负数、小数和科学计数法。
- 可选字段和缺失值。
- 非数值字段。
- 无命名捕获组。
- 非法正则和超时恢复。
- Unicode 前后缀。
- 正则修改后的系列配置迁移。

### 21.2 流式解码测试

- UTF-8 和 GBK 字符跨 chunk。
- CRLF 跨 chunk。
- 独立 CR 和 LF。
- 多行位于同一 chunk。
- 超长行。
- 断开和重连边界。

### 21.3 时间轴控制器测试

- 完整范围扩展。
- 实时窗口随最新数据移动。
- 用户平移后关闭实时跟随。
- 回到实时保留当前窗口时长。
- 左右手柄缩放。
- 最小窗口和完整范围边界。
- 点击定位。
- 主图缩放与时间轴窗口双向同步。
- 避免同步递归。
- 相同时间戳按序号稳定排序。

### 21.4 摘要层测试

- 时间桶 first/last/min/max/sum/count 正确。
- 多层摘要合并正确。
- 原始数据淘汰后完整范围不丢失。
- 时间轴按像素宽度选择层级。
- 尖峰在 min/max 包络中保留。
- 摘要和原始数据交界处无错误重叠。

### 21.5 工作区测试

- 图表页创建、关闭和恢复。
- Pane 间移动和排序。
- 重命名持久化。
- 清空只影响当前图表。
- 激活、分屏和窗口 resize。
- 关闭后释放两个 uPlot 实例、Worker、事件监听器和 ResizeObserver。

### 21.6 性能测试

建议基线：

```text
3 个系列
每秒 1,000 行
连续运行 30 分钟
原始数据最多 200,000 点
同时打开 4 个图表页
```

验收要求：

- Renderer 内存不持续无界增长。
- 时间轴拖动和缩放保持流畅。
- 新数据到达时历史窗口不被强制拉回实时位置。
- 隐藏标签不持续高频绘制。
- Worker 队列有界。
- 关闭标签后相关内存可回收。

## 22. 分阶段实施

### 第一阶段：文本记录基础设施

- 提取流式解码和按行分帧模块。
- 修复跨 chunk CRLF。
- 增加 session、sequence 和接收时间。
- 完成三级解析、样例字段发现、格式模板和正则验证单元测试。

### 第二阶段：图表标签骨架

- 增加 `chartTabs` 配置、ID 和生命周期。
- 完成设置界面、样例字段发现表和字段勾选流程。
- 接入静态 uPlot 主图和时间轴。

### 第三阶段：数据模型与时间轴

- 实现原始数据层。
- 实现分层时间摘要。
- 实现 `ChartViewportController`。
- 完成窗口拖动、缩放、定位和实时跟随。
- 实现主图与时间轴双向同步。

### 第四阶段：实时解析

- 接入文本记录流和解析 Worker。
- 实现多系列实时绘制。
- 实现隐藏标签降频和输入过载保护。

### 第五阶段：交互与导出

- 图例、Tooltip、统计和 Y 轴配置。
- 当前窗口和全部原始数据 CSV 导出。
- Context Menu、键盘操作和国际化。

### 第六阶段：稳定性

- 高频输入压测。
- Worker 超时恢复。
- 重连边界测试。
- 时间轴长会话测试。
- 内存泄漏检查。
- Windows 安装包验证。

## 23. 验收标准

1. 创建图表页并粘贴样例后，自动键值模式能识别 `a`、`b`、`c` 三个候选字段。
2. 粘贴 `[FRAME_PERF]` 样例后，工具自动列出 `count`、`frame_total`、`layout_prep`、`render_envelope`、`flush`、`draw`、`post_render`，并识别耗时字段单位为 `us`。
3. 用户可以勾选候选字段，只有勾选字段会生成不同颜色的折线。
4. `count` 默认不勾选，用户可以将其指定为样本序号或手动生成折线。
5. 串口持续输出匹配日志时，多条折线实时更新。
6. 底部时间轴始终覆盖当前会话完整范围。
7. 上方主图只显示底部滑动窗口覆盖的时间范围。
8. 拖动窗口主体能够平移主图时间范围。
9. 拖动左右手柄能够缩放窗口长度，并按比例改变主图显示时长。
10. 主图滚轮缩放和拖动会同步更新底部窗口。
11. 用户查看历史后，新数据不会强制改变当前窗口。
12. 点击“回到实时”后，窗口保持当前长度并移动到最新数据。
13. 原始数据淘汰后，底部时间轴仍保留完整会话趋势摘要。
14. 仅有摘要的历史范围会明确标记为降采样数据。
15. 主终端切换到 Hex 后，图表仍继续按自身编码解析。
16. 暂停和清空图表不影响其他串口功能。
17. 图表页移动、分屏、重命名和重启恢复正确。
18. CSV 只导出原始数据，并正确说明摘要历史不可精确导出。
19. 非法或高回溯正则不会阻塞 Renderer。
20. 高频输入时队列和内存保持有界，并显示丢弃计数。
21. 图表关闭后不再接收数据，所有资源均正确释放。

## 24. 关键设计结论

- 图表页必须消费串口文本记录流，不能解析 xterm 渲染内容。
- 自动键值是默认解析入口，格式模板是推荐的精确匹配方式，正则只作为高级能力。
- 样例日志自动发现字段，用户勾选后的字段定义才是系列的稳定契约。
- 主图和完整时间轴必须由统一视口控制器驱动。
- 底部窗口的像素宽度必须与主图时间跨度严格成比例。
- 完整时间轴采用分层摘要保留长期趋势，原始数据采用有界保留。
- 用户浏览历史时必须停止自动跟随，但不能停止后台接收和完整时间轴扩展。
- 所有实时日志解析放入有过载保护的 Worker，正则模式额外启用超时恢复。
- 第一版优先保证实时趋势、时间导航、性能边界和数据语义正确，再扩展报警、公式和离线导入。
